import React from 'react';
import {RootObject} from '../types/planets';


export type PlanetListProps = {
    allPosts: RootObject | null
}

const PlanetList:React.FC<PlanetListProps> = (allPlanets) => {
  
    
    

    return (
            <>
                 
            </>
           
       
    );
};

export default PlanetList;